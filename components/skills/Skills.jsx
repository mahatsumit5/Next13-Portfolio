import React from "react";
import SkillModal from "../modal/SkillModal";

const Skills = () => {
  return (
    <div>
      <button>Add new Skills</button>
      <SkillModal title={"Add  New Skills"} />
    </div>
  );
};

export default Skills;
